// Ensure the DOM is fully loaded before executing our logic
document.addEventListener('DOMContentLoaded', () => {
    
    // Initialize the application
    console.log("WanderMap application initialized.");
    console.log("HTML, CSS, and JavaScript are successfully linked!");

    // Basic setup for future logic will go here
});